# Generic Fallback Images

Universal fallback images when no specific image is available.

## Current Fallbacks
- Generic PC (`generic-pc.svg`) - Universal fallback for all listings

This is the last resort in the 7-level fallback hierarchy.
